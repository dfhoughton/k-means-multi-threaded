<!DOCTYPE html>
<html>
  <head>
    <title>K-Means Multi-Threaded</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- use mui's preferred font -->
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
    />
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <!--
      The browser will ignore this because it has an unfamiliar type.
      The id helps us find the node in application.tsx.
      The src attribute will be used there to build a worker.
    -->
    <script id="worker" type="text/js-worker" src=<%=asset_path('worker')%>></script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
